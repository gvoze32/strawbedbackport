{
  "schemaVersion": 1,
  "id": "${id}",
  "version": "${version}",
  "name": "${name}",
  "description": "${description}",
  "authors": [
    "gvoze32"
  ],
  "environment": "*",
  "entrypoints": {
    "main": [
      "${group}.StrawBedMod"
    ]
  },
  "mixins": [
    "strawbed.mixins.json"
  ],
  "depends": {
    "fabricloader": "*",
    "fabric-api": ">=${fabricVersion}",
    "minecraft": ">=${minecraftVersion}"
  }
}
